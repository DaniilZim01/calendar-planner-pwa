{
  "version": 2,
  "buildCommand": "npm install && cd client && npm install && npm run build && cd .. && xcopy client\\dist dist /E /I /Y",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite"
}
